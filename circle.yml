machine:
  timezone:
    Asia/Tokyo
  java:
    version: oraclejdk8
deployment:
  production:
    branch: master
    commands:
      - gradle bootRepackage
test:
  override:
    - gradle check test
  post:
    - mv build/reports build/libs/*.jar $CIRCLE_ARTIFACTS/
